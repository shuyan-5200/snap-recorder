# 更新记录

## 0.5.0（build 13）

- 视频、电脑声音、人声自由勾选；合并、分轨两种方式单选，支持只导出声音。有人声时默认分轨，未录人声时默认合并。
- 导出页简化为内容、输出方式、视频大小和名称，按钮去掉重复副标题，设置区增加细边框，输出方式改为紧凑控件，放弃和重录使用边框按钮。
- 主面板可正常截图，整屏或局部录制时主动打开主面板可以将它录入；倒计时、HUD、摄像头预览和选区面板仍被排除。
- 增加高清、日常、小巧、极小与自定义大小，预估体积并验证自定义 MP4 上限，避免高清压缩增大文件。
- 保存前命名，保存后继续补导出；允许取消、放弃和直接重新录制，同名文件自动编号。
- 导出失败、取消与补导出保留原片；结束会话只清理临时来源，不影响已存文件。

## 0.4.0（build 11）

- 修复按住“自然修饰”强度滑杆拖动时，整个窗口跟着移动的问题：内容区不再作为拖动窗口的背景，窗口仍可从顶部标题栏移动。
- 将连续滑杆改为“原图 / 自然 / 柔和”三档，默认原图；扩大内侧脸颊柔化范围，重新校准两档降噪和混合强度，使档位有实际区别，继续保护五官与背景。
- 设置中说明正脸检测条件，检测失败时保持原图。不增加瘦脸、美妆、费用或网络请求，采集与导出流程不变；本版本已作为 GitHub Release 发布，验收结果见 `docs/verification.md`。

### build 10：自然修饰与人像位置设置

- 新增默认关闭的“自然修饰”，使用免费的苹果原生 Core Image 与 Vision；开启后以默认 35% 的轻强度柔化人脸区域肤质，不瘦脸、不美妆、不改变五官形状。
- 自然修饰只在本机内存中检测人脸区域；不识别身份，不保存人脸模型、特征向量或检测标记，也不引入网络请求或第三方依赖。
- 摄像头预览与编码共用处理后的画面，修饰仅作用于摄像头，不影响桌面内容。
- 人像设置移入主窗口，四角位置改为 2×2 整卡可点击选项，修复开启摄像头后位置选项点不动的问题。
- 本构建的安装、自动自检和真实设备验收结果以 `docs/verification.md` 为准；以下 build 9 记录仅为历史结果，不代表 build 10 已通过验收。本版本尚未发布到 GitHub。

### build 9：摄像头画中画与历史验收

- 新增默认关闭的“摄像头”开关，与人声、电脑声音独立；开启后先显示本地预览，再随录屏合入成片。
- 默认使用右下角、镜像开启的圆角方形人像；提供圆形 / 圆角方形、三档大小、四角位置与镜像轻设置。
- 浏览器窗口、整个屏幕和局部录像共用画中画能力；预览浮窗不可共享，不会在成片里重复出现。
- 保持原有画质与人声导出流程，不新增摄像头分轨；最高画质的人声混合继续透传视频轨。
- 关闭开关、停止录制或闲置时关闭主窗口会释放摄像头；设备异常会提示原因，录制中意外断开时停止并尽力保存已录内容。
- 更新摄像头权限与纯本地处理说明；功能已实现并安装为本机体验版，Universal 双架构、签名和安装后完整自检通过。
- 浏览器窗口与真实摄像头的最高画质录制已通过：关闭电脑声音和麦克风时只导出一个 MP4，右下角圆角人像正常显示，静止网页下人像持续更新，停止后释放摄像头。
- 左上角小圆形人像的真实浏览器录制与“清晰小体积”导出已通过，解码回看确认位置、形状与大小正确。
- 两次实录通过退出保护中的“结束并保存”进入导出；录制控制条按钮、真实暂停 / 继续、整屏和局部摄像头、真实声音组合及设备异常场景仍待验收。完整结果和范围见 `docs/verification.md`，本版本未发布到 GitHub。

## 0.3.1

- 新增“局部录像”第三种来源，支持 16:9、9:16、4:3、3:4、21:9、1:1 与自定义比例的可拖动屏幕选区。
- 大录制框与固定比例下的聚焦蒙版都可选方角或圆角并默认圆角；蒙版框内保持原色，框外转为单色并压暗 50%。
- 新增可选柔和圆角暗角，并补充比例、蒙版与暗角的像素级自检。
- 新增状态化快捷键：局部录像用 `⌘E` 锁定点击穿透浮层，锁定后可全局 `⌘R` 开录；任意模式录制中可全局 `Esc` 结束。
- 新增三个模式共用的“录制鼠标”开关：默认以带光晕的圆形光点替代系统箭头，点击时显示扩散波纹；关闭后成片不含鼠标。
- 修复本地重新构建后屏幕录制权限反复失效的问题，并阻止授权页重复触发系统授权请求。
- 基于当前 `main` 重新构建并发布 Universal App，使下载包与最新 README、隐私说明保持一致。

## 0.3.0

- 结束录制后可选择“最高画质 / 清晰小体积”；最高画质直接保留原片，小体积档保持原分辨率，以约 1/3 视频目标码率和更高压缩效率尽量保留清晰度。
- 浏览器录制改为完整窗口直接铺满成片，不再添加桌面壁纸、渐变、圆角、阴影或额外边距。
- 提高倒计时与录制计时 HUD 的窗口层级，确保在浏览器全屏空间中可见，同时继续保持不可共享。

## 0.2.0

- 新增可选“人声（麦克风）”，默认关闭，仅在用户主动开启时申请权限。
- 未录人声时保持原有一键自动保存；录了人声后可以单选或同时选择“完整视频”与“视频 + 人声分轨”。
- 系统声与人声使用独立音轨和统一时间轴；暂停区间会同时从画面、电脑声音和人声中移除。
- 合并导出只重新混合音频，H.264 视频码流原样透传，不降低画质。
- 高清 H.264 参数会先检查当前系统编码器是否支持；旧版 macOS 会安全回退，避免启动录制时崩溃。
- 分开导出会补齐人声首尾静音，使 M4A 与视频时长误差控制在 40 毫秒内。
- 人声为 AAC-LC 48 kHz / 192 kbps；合并音频为 48 kHz / 256 kbps。
- 新增等待导出时的退出保护、最多 3 个文件的访达定位、统一重名保护、麦克风权限说明与失败重试。
- 产品正式更名为 Snap Recorder，公开仓库准备为 `snap-recorder`，应用标识为 `io.github.shuyan-5200.SnapRecorder`。
- 增加开源 README、隐私说明、贡献指南、MIT License 与 GitHub Actions 自检。

## 0.1.4

- 浏览器与整屏模式改为原生像素优先，最高支持 3840×2160；当前 Retina 浏览器不再预先缩到 1440 高。
- 浏览器内容框改为整数像素对齐，禁止为填补舍入误差做轻微上采样，提升网页文字和细线清晰度。
- H.264 质量预算提升到约 24–68 Mbps，启用最高采集质量、CABAC 和编码质量优先。
- 高清录制前的剩余空间保护由 2 GB 提升到 5 GB。

## 0.1.3

- 浏览器成片不再固定为 16:9，改为跟随开始录制时的浏览器窗口比例，避免视觉上横向偏扁。
- 浏览器输出最高约 2K，窗口约占画布 95% 宽高，只保留一圈轻微壁纸背景。
- 缩小阴影范围，并补充比例、边距、偶数像素和整屏尺寸回归自检。

## 0.1.2

- 修复关闭主窗口后再次双击 Snap Recorder，却没有重新显示窗口的问题。
- 统一为单一安装入口，避免多份 Snap Recorder 同时运行造成入口与权限混乱。

## 0.1.1

- 修复可见浏览器窗口被旧 `isOnScreen` 状态误判为最小化、导致无法开始录制的问题。
- Snap Recorder 回到前台时自动刷新浏览器窗口；已选窗口关闭后要求重新选择，不会静默切换目标。
- 正式进入录制状态前等待首个完整画面；无画面时约 3 秒内返回明确提示。
- 停止采集后才重新显示 Snap Recorder，避免整屏成片末帧混入菜单栏状态。
- 缓存浏览器壁纸与阴影合成，降低 1080p 录制时的实时处理压力。
- “系统声音”改为“电脑声音（不含麦克风）”，减少理解歧义。

## 0.1.0

- 首个可运行 MVP：浏览器窗口 / 整个主屏幕、3 秒倒计时、暂停继续、自动导出 MP4。
