# Snap Recorder 项目规则

- 产品目标是极简录屏，不新增编辑器、剪辑、变焦、云分享等重功能。
- 默认中文界面，流程必须保持“选来源 → 倒计时 → 录制 → 选择导出画质 → 自动保存”。
- 浏览器模式必须坚持独立窗口采集，按窗口原始比例完整生成画面，不添加假桌面，也不能退化成屏幕区域裁切。
- 整屏模式必须排除 Snap Recorder 自身应用；倒计时和录制 HUD 必须保持不可共享。
- 输出固定保存到下载目录，不加入保存面板。
- 人声默认关闭；未开启时结束后只选择“最高画质 / 清晰小体积”，随后保存一个 MP4。
- 摄像头默认关闭，独立于人声开关；开启后本地预览，默认右下角圆角人像。仅用户授权后采集，关闭开关、录制结束或空闲时关闭主窗口必须释放摄像头。
- 人像在编码前直接叠入屏幕视频；预览窗不可共享、不可重复录入；静止屏幕、暂停和继续也必须保持人像更新与音画同步，不新增摄像头分轨或编辑器。
- 自然修饰默认关闭，使用免费的苹果原生能力，只处理摄像头画面；预览与编码必须共用处理结果。不瘦脸、不美妆、不识别身份，区域检测仅在开启时于本机内存中进行，不保存人脸模型、特征向量或检测标记。
- 开启人声后在画质选择之外，只提供“完整视频 / 视频与人声分轨”两张可多选导出卡，不增加格式或设备选择器。
- 合并人声时不得重新编码视频轨；分开导出不得打 ZIP。
- 实际代码修改后至少运行 `swift build -c release` 与 `.build/release/SnapRecorder --self-test`。
- 首次涉及真实屏幕、麦克风或摄像头采集的验证，需要用户在 macOS 系统提示中批准对应权限。
- 完整 Xcode 不是必需条件；可以使用 SwiftPM 与 `scripts/build-app.sh` 构建 `.app`。
